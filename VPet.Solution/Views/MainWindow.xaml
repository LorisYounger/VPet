<pu:WindowX
  x:Class="VPet.Solution.Views.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ll="clr-namespace:LinePutScript.Localization.WPF;assembly=LinePutScript.Localization.WPF"
  xmlns:local="clr-namespace:VPet.Solution"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:pu="https://opensource.panuon.com/wpf-ui"
  xmlns:system="clr-namespace:System;assembly=mscorlib"
  xmlns:vm="clr-namespace:VPet.Solution.ViewModels"
  Title="{ll:Str 'VPET 问题解决工具'}"
  Width="400"
  Height="450"
  MinWidth="400"
  MinHeight="200"
  d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
  WindowStartupLocation="CenterScreen"
  mc:Ignorable="d"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" MinHeight="200" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Label
      Height="Auto"
      Content="{ll:Str 'VPET 问题解决工具'}"
      FontSize="32"
      Style="{DynamicResource Label_BaseStyle}"
    />
    <ComboBox
      x:Name="Languages"
      Grid.Column="1"
      MinWidth="150"
      HorizontalAlignment="Right"
      VerticalAlignment="Top"
      ItemsSource="{Binding AvailableCultures}"
      SelectedItem="{Binding CurrentCulture}"
      Style="{DynamicResource ComboBox_BaseStyle}"
    >
      <ComboBox.ContextMenu>
        <ContextMenu
          d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
          DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
        >
          <MenuItem
            x:Name="OpenLocalText"
            Header="{ll:Str 打开翻译文本}"
            Command="{Binding OpenLocalTextCommand}"
          />
        </ContextMenu>
      </ComboBox.ContextMenu>
    </ComboBox>
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <StackPanel>
        <Button
          x:Name="OpenSettingEditor"
          MinWidth="150"
          Content="{ll:Str 打开设置编辑器}"
          FontSize="16"
          Command="{Binding OpenSettingCommand}"
          Style="{DynamicResource Button_BaseStyle}"
        />
        <!--<Button
          x:Name="OpenSaveViewer"
          MinWidth="150"
          Click="OpenSaveViewer_Click"
          Content="{ll:Str 打开存档查看器}"
          FontSize="16"
          Style="{DynamicResource Button_BaseStyle}"
        />-->
      </StackPanel>
      <Button
        x:Name="FirstStartFailed"
        Grid.Row="1"
        Height="30"
        Command="{Binding FirstStartFailedCommand}"
        Style="{DynamicResource LinkButtonStyle}"
        Content="{ll:Str '第一次启动桌宠打不开?'}"
        FontSize="16"
      />
    </Grid>
  </Grid>
</pu:WindowX>
