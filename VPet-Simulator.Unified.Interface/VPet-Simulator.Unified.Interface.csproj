<Project Sdk="Microsoft.NET.Sdk">

	<!--
	统一 MOD 契约.

	Windows 版和跨平台版加载的是**同一个 dll**, 因此一个按这份契约写的 MOD,
	编译一次两边都能跑. 旧的 MainPlugin 体系原样保留作兼容层, 不受影响.

	== 硬约束: 自包含 ==

	只能引用 BCL 和 LinePutScript, 不能引用任何其它工程:
	  - Core.Base / Windows.Interface.Base 是"共享源码"而不是共享程序集,
	    IGameSave / Food / GameSave_v2 这些类型在 Core.dll 和 Base.dll 里是两个
	    不同的 CLR 类型. 契约一旦引用其中一份, 就只能给一边用了.
	  - Localization 也不能引: Windows 用的是 .WPF 版, 跨平台用的是非 WPF 版,
	    两个不同的程序集. 翻译走宿主门面的 Translate.
	  - 更不用说 Avalonia 和 WPF.
	LinePutScript 是例外: 两边用的确实是同一个包同一个版本.

	== 硬约束: 公开表面冻结 ==

	MOD 是以编译好的 dll 分发的, 所以这里的公开表面与
	VPet-Simulator.Windows.Interface.dll 受同样的约束: **只许增, 不许改, 不许减**.
	ABI-Compatibility.md 里的 R1/R2/R3/N6 逐字适用. 另外两个宿主的加载器都只认
	"直接派生自 UnifiedPlugin 一层", 所以永远不要在中间插基类, 也不要把它改成接口.
	-->

	<PropertyGroup>
		<TargetFramework>net8.0</TargetFramework>
		<RootNamespace>VPet_Simulator.Unified.Interface</RootNamespace>
		<AssemblyName>VPet-Simulator.Unified.Interface</AssemblyName>
		<Nullable>enable</Nullable>
		<LangVersion>latest</LangVersion>
		<ImplicitUsings>disable</ImplicitUsings>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<NoWarn>CS1591</NoWarn>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\.editorconfig" Link=".editorconfig" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="LinePutScript" Version="1.11.9" />
	</ItemGroup>

</Project>
