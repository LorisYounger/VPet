<Project Sdk="Microsoft.NET.Sdk">

	<!--
	统一层: Windows 版和跨平台版加载的是**同一个 dll**. 两个目录, 两种约束:

	  Interface\  统一 MOD 契约 (命名空间 VPet_Simulator.Unified.Interface)
	  Services\   统一后端     (命名空间 VPet_Simulator.Unified.Services)

	== Interface: 契约 ==

	一个按这份契约写的 MOD, 编译一次两边都能跑. 旧的 MainPlugin 体系原样保留作兼容层, 不受影响.
	MOD 是以编译好的 dll 分发的, 所以 Interface 命名空间的公开表面与
	VPet-Simulator.Windows.Interface.dll 受同样的约束: **只许增, 不许改, 不许减**.
	ABI-Compatibility.md 里的 R1/R2/R3/N6 逐字适用. 另外两个宿主的加载器都只认
	"直接派生自 UnifiedPlugin 一层", 所以永远不要在中间插基类, 也不要把它改成接口.

	== Services: 后端 ==

	存在的理由是"一份后端两边用": 存档管理、备份恢复、存档索引、MOD 元数据、
	资源查找、统计汇总这些业务逻辑以前只有 Windows 版有, 跨平台版要用就得抄一遍,
	抄完两边就开始各自演化. 以后加新功能(比如新的存档管理)只写一份, 两个界面都能用.
	Services 命名空间**不是契约**: 它只给两个宿主用, MOD 不许引用 (仓库外的统一契约门禁
	会检查示范 MOD 的类型引用表), 所以可以随宿主一起改.

	== 硬约束: 自包含 ==

	整个程序集只能引用 BCL 和 LinePutScript, 不能引用任何其它工程:
	  - Core.Base / Windows.Interface 的 Base 目录是"共享源码"而不是共享程序集,
	    IGameSave / Food / GameSave_v2 这些类型在 Core.dll 和跨平台宿主里是两个
	    不同的 CLR 类型. 这里一旦引用其中一份, 就只能给一边用了.
	    所以所有服务只操作 ILPS / ILine / 路径 / 自己定义的 DTO.
	  - Localization 也不能引: Windows 用的是 .WPF 版, 跨平台用的是非 WPF 版,
	    两个不同的程序集. 翻译走宿主门面的 Translate.
	  - 更不用说 Avalonia 和 WPF: 这里是纯业务, 界面在各自宿主里.
	LinePutScript 是例外: 两边用的确实是同一个包同一个版本.

	2026-09-16 起由 VPet-Simulator.Unified.Interface 与 VPet-Simulator.Unified.Services 两个工程合并而来,
	命名空间不变, 只是 dll 名变成 VPet-Simulator.Unified.
	-->

	<PropertyGroup>
		<TargetFramework>net8.0</TargetFramework>
		<RootNamespace>VPet_Simulator.Unified</RootNamespace>
		<AssemblyName>VPet-Simulator.Unified</AssemblyName>
		<Nullable>enable</Nullable>
		<LangVersion>latest</LangVersion>
		<!-- 与 Core.Base 一致: Core 没开隐式 using, 共享代码的行为必须一致 -->
		<ImplicitUsings>disable</ImplicitUsings>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<NoWarn>CS1591</NoWarn>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\.editorconfig" Link=".editorconfig" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="LinePutScript" Version="1.11.9" />
	</ItemGroup>

</Project>
